public class Solution {
    public static int getNearestMulitple(int n) {
        int remainder = n % 10;
        
        if (remainder <= 5) {
            return n - remainder;
        } else {
            return n + (10 - remainder);
        }
    }
}
