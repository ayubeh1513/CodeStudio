import java.util.* ;
import java.io.*; 
public class Solution {
    public static Boolean splitString(String str) {
        // Write your code here..
        int r = 0, l = 0; //rb = 0, lb = 0;
        for(int i=0; i<str.length(); i++){
            if((i<(str.length())/2) && (str.charAt(i)=='a' || str.charAt(i)=='i' || str.charAt(i)=='e' || str.charAt(i)=='u' || str.charAt(i)=='o' || str.charAt(i)=='A' || str.charAt(i)=='I' || str.charAt(i)=='E' || str.charAt(i)=='U' || str.charAt(i)=='O')){
                r++;
            }

            if((i>=(str.length())/2) && (str.charAt(i)=='a' || str.charAt(i)=='i' || str.charAt(i)=='e' || str.charAt(i)=='u' || str.charAt(i)=='o' || str.charAt(i)=='A' || str.charAt(i)=='I' || str.charAt(i)=='E' || str.charAt(i)=='U' || str.charAt(i)=='O')){
                l++;
            }

            // if((i<=(str.length())/2) && (str.charAt(i)=='A' || str.charAt(i)=='I' || str.charAt(i)=='E' || str.charAt(i)=='U' || str.charAt(i)=='O')){
            //     rb++;
            // }

            // if((i>(str.length())/2) && (str.charAt(i)=='A' || str.charAt(i)=='I' || str.charAt(i)=='E' || str.charAt(i)=='U' || str.charAt(i)=='O')){
            //     lb++;
            // }
        }
        if(l==r) return true;
        return false;
    }
}
